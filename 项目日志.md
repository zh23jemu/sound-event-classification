# 声音事件分类项目日志

## 使用说明

本日志用于记录项目研究和实验过程。每次阅读文献、修改代码、训练模型或解决问题后，都建议新增一条记录，保证最终报告中可以追溯实验过程。

## 日志模板

### 日期：YYYY-MM-DD

**今日目标**

- 

**完成内容**

- 

**遇到的问题**

- 

**解决方案**

- 

**实验记录**

| 实验编号 | 数据集 | 模型 | 配置 | 指标 | 输出位置 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

**阶段结论**

- 

**下一步计划**

- 

## 2026-06-08

**今日目标**

- 从文档阶段进入代码实现阶段，建立 ESC-50 baseline 的最小可运行工程。

**完成内容**

- 新增项目日志模板。
- 新增 ESC-50 数据检查脚本、训练配置、数据读取、Log-Mel 特征、CNN baseline、训练和评估代码骨架。

**遇到的问题**

- 当前仓库尚未包含 ESC-50 数据，训练脚本暂时只能在数据准备后运行完整训练。
- 使用 `.venv\Scripts\python.exe -m pip install -r requirements.txt` 安装依赖时，本次命令在 120 秒后超时，尚未完整装好 `torch`、`pandas` 等依赖。

**解决方案**

- 先提供 `scripts/prepare_esc50.py` 检查数据目录结构，后续下载 ESC-50 后再运行训练。
- 已完成 Python 语法编译检查，确认新增脚本和模块没有语法错误；后续需要继续完成依赖安装。

**实验记录**

| 实验编号 | 数据集 | 模型 | 配置 | 指标 | 输出位置 |
| --- | --- | --- | --- | --- | --- |
| E000 | ESC-50 | CNN baseline | `configs/esc50_baseline.yaml` | 待运行 | `outputs/esc50_baseline` |

**阶段结论**

- 项目已具备进入 baseline 实验的代码框架。

**下一步计划**

- 下载并放置 ESC-50 数据，运行数据检查脚本。
- 继续安装 `requirements.txt` 中的依赖；如在 GPU 集群运行，优先按 README 中 CUDA 12.x wheel 方式安装 PyTorch。
- 运行 CNN baseline 小规模训练，确认数据读取、特征维度和训练日志正常。

## 2026-06-08 服务器首次运行记录

**今日目标**

- 在 Slurm 登录节点同步项目、下载 ESC-50，并提交 CPU baseline 任务。

**完成内容**

- ESC-50 已在服务器 `data/ESC-50` 目录检查通过，共 2000 个音频文件。
- baseline 任务已成功切换到 `ISP` 分区运行。

**遇到的问题**

- 训练启动后在 `torchaudio.load()` 读取音频时报错：新版 `torchaudio` 需要 TorchCodec/FFmpeg，安装 `torchcodec` 后又出现 `libnvrtc.so.13` 运行库不匹配问题。

**解决方案**

- 将 ESC-50 WAV 读取逻辑改为 `scipy.io.wavfile.read`，避开 `torchaudio.load()` 的 TorchCodec 路径。
- `requirements.txt` 移除 `torchcodec`，显式补充 `scipy`。

**实验记录**

| 实验编号 | 数据集 | 模型 | 配置 | 指标 | 输出位置 |
| --- | --- | --- | --- | --- | --- |
| E001 | ESC-50 | CNN baseline | `configs/esc50_baseline.yaml` | 运行失败：TorchCodec/运行库不匹配 | `outputs/slurm/esc50-baseline-34855889.*`、`outputs/slurm/esc50-baseline-34855928.*` |

**阶段结论**

- 数据目录和 Slurm 分区选择已验证，下一步需要同步 SciPy WAV 读取修复后重跑 baseline。

## 2026-06-08 ESC-50 baseline 首次完整训练

**今日目标**

- 验证修复后的音频读取逻辑能否在服务器 Slurm 环境中完成 ESC-50 baseline 训练。
- 获得第一版可报告的 CNN baseline 指标，为后续增强方法和 AST/ViT 迁移实验提供对照。

**完成内容**

- 使用服务器 `data/ESC-50` 数据目录完成 20 epoch 训练。
- 作业运行在 `ISP` 分区，任务编号为 `34857536`。
- 训练脚本成功保存最佳模型到 `outputs/esc50_baseline/best_model.pt`。

**遇到的问题**

- 本轮没有再出现 TorchCodec、FFmpeg 或 CUDA 运行库相关错误。
- `tqdm` 进度条输出到 Slurm `.err` 文件属于正常进度显示，不代表训练失败。

**解决方案**

- 沿用 `scipy.io.wavfile.read` 读取 ESC-50 WAV 文件的修复方案，绕开 `torchaudio.load()` 在当前服务器环境中触发的 TorchCodec 解码依赖。

**实验记录**

| 实验编号 | 数据集 | 模型 | 配置 | 结果 | 输出 |
| --- | --- | --- | --- | --- | --- |
| E002 | ESC-50 | CNN baseline | `configs/esc50_baseline.yaml` | 20 epoch 完成；最佳验证 Accuracy = 0.4125；第 19 轮验证 Loss = 2.0202 | `outputs/esc50_baseline/best_model.pt`、`outputs/slurm/esc50-baseline-34857536.*` |

**阶段结论**

- ESC-50 + Log-Mel Spectrogram + 轻量 CNN baseline 已经跑通，验证准确率明显高于 50 类随机水平约 0.02，说明当前数据管线和训练流程有效。
- 训练集准确率最高约 0.4688、验证集最高约 0.4125，当前模型仍是较弱 baseline，后续有明确改进空间。

**下一步计划**

- 将服务器上的 `outputs/esc50_baseline/history.json` 和 `latest_val_metrics.json` 同步回本地，便于画训练曲线和写报告。
- 增加类别级指标、混淆矩阵或错误样本分析，支撑后续文档中的 baseline 结果讨论。
- 在 baseline 基础上开展改进比较：优先尝试 SpecAugment / mixup / 随机裁剪，再接入预训练 AST/ViT 做主线对比。

## 2026-06-08 ESC-50 baseline 结果分析

**今日目标**

- 基于服务器同步回本地的 `history.json` 和 `latest_val_metrics.json` 生成训练曲线、混淆矩阵、类别级指标和结果摘要。

**完成内容**

- 新增 `scripts/analyze_esc50_results.py`，用于复用性分析 ESC-50 baseline 输出。
- 生成训练 Loss 曲线、训练 Accuracy 曲线、归一化混淆矩阵、类别级指标 CSV 和 Markdown 摘要。
- 输出目录为 `outputs/esc50_baseline/analysis`。

**遇到的问题**

- 本地没有同步 ESC-50 原始数据和 `meta/esc50.csv`，因此当前图表和摘要暂时使用 `class_00` 到 `class_49` 的类别编号。

**解决方案**

- 分析脚本支持传入或默认读取 `data/ESC-50/meta/esc50.csv`；如果后续本地同步元数据，重新运行脚本即可显示 ESC-50 真实类别名。

**实验记录**

| 实验编号 | 数据集 | 模型 | 分析输入 | 结果 | 输出 |
| --- | --- | --- | --- | --- | --- |
| E003 | ESC-50 | CNN baseline | `history.json`、`latest_val_metrics.json` | 最佳验证 Accuracy = 0.4125；类别级指标和混淆矩阵已生成 | `outputs/esc50_baseline/analysis` |

**阶段结论**

- 训练曲线显示 baseline 整体在 20 轮内持续学习，验证 Accuracy 在第 19 轮达到 0.4125，最后一轮小幅回落。
- 类别级指标显示不同类别差异明显，部分类别达到 0.8750 或 1.0000，部分类别为 0，后续改进需要关注类别混淆和泛化能力。

**下一步计划**

- 同步或保留 ESC-50 元数据，以便图表显示真实类别名称。
- 使用分析图和摘要补充报告中的 baseline 实验结果部分。
- 继续实现数据增强对比实验，优先比较原始 CNN baseline 与 SpecAugment / mixup / 随机裁剪版本。

## 2026-06-08 SpecAugment 对比实验准备

**今日目标**

- 在保持 CNN baseline 不变的前提下，新增第一个改进比较实验：CNN + SpecAugment。

**完成内容**

- 在 `src/sound_event_classification/features.py` 中新增 `SpecAugment` 模块，支持频率遮挡和时间遮挡。
- 更新 `scripts/train.py`，在训练阶段将 SpecAugment 插入 Log-Mel 特征之后、CNN 输入之前；验证阶段不启用增强。
- 更新 `configs/esc50_baseline.yaml`，显式保持 baseline 不启用增强。
- 新增 `configs/esc50_cnn_specaugment.yaml`，输出目录为 `outputs/esc50_cnn_specaugment`。
- 更新 Slurm 脚本，支持通过 `CONFIG` 环境变量切换训练配置。

**遇到的问题**

- 当前本地没有 ESC-50 原始数据，因此无法在本地完成真实训练，只能做语法、配置和张量级 smoke test。

**解决方案**

- 使用 `.venv\Scripts\python.exe -m compileall scripts src` 验证脚本语法。
- 使用随机 Log-Mel 张量验证 SpecAugment 输出形状不变，遮挡逻辑可运行。
- 使用配置加载器验证 baseline 与 SpecAugment 两个 YAML 都能正常读取。

**实验记录**

| 实验编号 | 数据集 | 模型 | 配置 | 状态 | 输出 |
| --- | --- | --- | --- | --- | --- |
| E004 | ESC-50 | CNN + SpecAugment | `configs/esc50_cnn_specaugment.yaml` | 代码已准备，待服务器训练 | `outputs/esc50_cnn_specaugment` |

**阶段结论**

- 当前项目已具备第一个“改进比较”实验入口，可以和 E002 的 CNN baseline 直接比较。
- SpecAugment 是训练时频谱增强，不改变模型结构，适合作为 baseline 后的低风险改进实验。

**下一步计划**

- 在服务器运行 `sbatch --partition=ISP --export=ALL,CONFIG=configs/esc50_cnn_specaugment.yaml slurm/train_esc50_baseline.sbatch`。
- 训练完成后同步 `outputs/esc50_cnn_specaugment/history.json` 和 `latest_val_metrics.json`，再运行分析脚本生成对比图表。

## 2026-06-08 SpecAugment 训练结果与对比

**今日目标**

- 拉取服务器端 CNN + SpecAugment 训练结果，生成分析产物，并与 CNN baseline 做第一轮改进比较。

**完成内容**

- 从远端同步 `outputs/esc50_cnn_specaugment/history.json` 和 `latest_val_metrics.json`。
- 使用分析脚本生成 SpecAugment 的训练曲线、归一化混淆矩阵、类别级指标和摘要。
- 新增 `scripts/compare_esc50_experiments.py`，用于对比多个实验的最佳验证 Accuracy、验证 Loss 和最后一轮指标。
- 生成 `outputs/esc50_comparison/summary.md` 和 `comparison.csv`。

**实验记录**

| 实验编号 | 数据集 | 模型 | 配置 | 最佳验证 Accuracy | 输出 |
| --- | --- | --- | --- | --- | --- |
| E002 | ESC-50 | CNN baseline | `configs/esc50_baseline.yaml` | 0.4125 | `outputs/esc50_baseline` |
| E004 | ESC-50 | CNN + SpecAugment | `configs/esc50_cnn_specaugment.yaml` | 0.4200 | `outputs/esc50_cnn_specaugment` |

**阶段结论**

- SpecAugment 的最佳验证 Accuracy 为 0.4200，相比 CNN baseline 的 0.4125 提升 +0.0075。
- SpecAugment 的最后一轮训练 Accuracy 为 0.4006，低于 baseline 的 0.4688，说明增强确实提高了训练难度。
- 当前提升幅度较小，适合作为“初步正向改进”，但不足以单独构成强结论；后续需要继续做增强强度调整、多折验证或预训练 AST/ViT 对比。

**下一步计划**

- 可以先尝试调小 SpecAugment 强度，观察最后几轮回落是否减轻。
- 更重要的下一阶段是接入预训练 AST/ViT，形成 CNN baseline、增强 CNN 和 Transformer 微调的主线对比。

## 2026-06-08 预训练 AST 微调入口准备

**今日目标**

- 接入预训练 Audio Spectrogram Transformer，推进项目主线从 CNN baseline / 数据增强比较进入 Transformer/ViT 微调比较。

**完成内容**

- 新增 `configs/esc50_ast.yaml`，配置 ESC-50 AST 微调实验。
- 新增 `scripts/train_ast.py`，使用 Hugging Face `AutoFeatureExtractor` 和 `AutoModelForAudioClassification` 加载预训练 AST。
- 新增 `slurm/train_esc50_ast.sbatch`，默认使用 `gpu` 分区、1 张 GPU、32G 内存和 6 小时时限。
- 更新 `requirements.txt`，补充 `transformers` 和 `accelerate`。
- 更新 README，补充 AST 本地运行、服务器依赖安装和 Slurm 提交说明。

**遇到的问题**

- 本地没有 ESC-50 原始数据，且未安装 Transformers 依赖，因此不能在本地做真实 AST 前向训练。
- AST 模型权重较大，首次服务器运行需要联网下载 Hugging Face 模型；如果计算节点不能联网，可能需要先在登录节点预下载缓存。

**解决方案**

- 将 AST 训练做成独立脚本，不影响已跑通的 CNN baseline 和 SpecAugment 实验。
- 脚本只在实际运行 AST 时导入 Transformers，避免普通 CNN 检查被额外依赖阻塞。
- AST 输出仍保持 `history.json` 和 `latest_val_metrics.json` 格式，后续可复用现有分析和对比脚本。

**实验记录**

| 实验编号 | 数据集 | 模型 | 配置 | 状态 | 输出 |
| --- | --- | --- | --- | --- | --- |
| E005 | ESC-50 | Pretrained AST fine-tuning | `configs/esc50_ast.yaml` | 代码已准备，待服务器 GPU 训练 | `outputs/esc50_ast` |

**阶段结论**

- 项目已具备三条对比线：CNN baseline、CNN + SpecAugment、预训练 AST 微调。
- AST 实验更贴近项目计划中的 Transformer/ViT 主线，是下一步最重要的实验。

**下一步计划**

- 在服务器执行 `git pull` 后安装缺失依赖：`.venv/bin/python -m pip install transformers accelerate`。
- 提交 AST GPU 任务：`sbatch slurm/train_esc50_ast.sbatch`。
- 训练完成后同步 `outputs/esc50_ast/history.json` 和 `latest_val_metrics.json`，加入现有对比摘要。

## 2026-06-08 预训练 AST 微调结果

**今日目标**

- 拉取服务器端预训练 AST 微调结果，生成分析产物，并更新三组实验对比。

**完成内容**

- 从远端同步 `outputs/esc50_ast/history.json` 和 `latest_val_metrics.json`。
- 生成 `outputs/esc50_ast/analysis` 下的训练曲线、归一化混淆矩阵、类别级指标和摘要。
- 更新 `outputs/esc50_comparison/summary.md` 和 `comparison.csv`，纳入 CNN baseline、CNN + SpecAugment、Pretrained AST 三组实验。

**实验记录**

| 实验编号 | 数据集 | 模型 | 配置 | 最佳验证 Accuracy | 最佳轮次 | 输出 |
| --- | --- | --- | --- | --- | --- | --- |
| E002 | ESC-50 | CNN baseline | `configs/esc50_baseline.yaml` | 0.4125 | 19 | `outputs/esc50_baseline` |
| E004 | ESC-50 | CNN + SpecAugment | `configs/esc50_cnn_specaugment.yaml` | 0.4200 | 19 | `outputs/esc50_cnn_specaugment` |
| E005 | ESC-50 | Pretrained AST fine-tuning | `configs/esc50_ast.yaml` | 0.9300 | 4 | `outputs/esc50_ast` |

**阶段结论**

- 预训练 AST 最佳验证 Accuracy 达到 0.9300，相比 CNN baseline 的 0.4125 提升 +0.5175。
- AST 第 4 轮达到最佳验证 Accuracy，最后一轮仍保持 0.9225，说明预训练迁移在 ESC-50 小数据集上效果显著。
- 当前三组实验已经形成清晰主线：从头训练 CNN 可跑通但较弱；SpecAugment 仅带来轻微提升；AudioSet 预训练 AST 显著提升分类性能。

**下一步计划**

- 将三组实验结果写入报告的实验设计与结果分析部分。
- 如果时间允许，可补充 AST 的类别级错误分析，或尝试多 fold / 调低 epoch 做稳定性验证。

## 2026-06-08 实验结果分析章节撰写

**今日目标**

- 将 CNN baseline、CNN + SpecAugment 和 Pretrained AST 三组结果整理成可直接进入最终报告的实验结果分析章节。

**完成内容**

- 新增 `实验结果分析.md`。
- 文档包含实验目的、数据集与划分、三组实验设置、整体结果表、训练曲线分析、混淆矩阵与类别级表现、讨论、局限性和后续工作。
- 明确当前核心结论：Pretrained AST 最佳验证 Accuracy 为 0.9300，相比 CNN baseline 的 0.4125 提升 +0.5175，是项目最强主线结果。

**遇到的问题**

- 本地没有 ESC-50 元数据，当前类别级图表仍以 `class_00` 到 `class_49` 显示，不适合直接做真实类别名称层面的错误分析。

**解决方案**

- 在文档中明确说明该限制，并将“同步元数据、重新生成真实类别名混淆矩阵”列为后续工作。

**阶段结论**

- 实验部分已经具备最终报告主体雏形，可以支撑“从 baseline 到增强，再到预训练 AST”的完整叙事。
- 当前最适合继续推进的是报告整合，而不是盲目增加新模型实验。

**下一步计划**

- 将 `实验结果分析.md` 与已有 `文献综述.md`、`项目计划.md` 进一步整合为完整模型报告初稿。
- 如需要提升图表可读性，优先同步 ESC-50 `meta/esc50.csv` 并重新生成带真实类别名的图表。

## 2026-06-08 模型报告初稿整合

**今日目标**

- 将文献背景、项目计划和实验结果分析整合为完整模型报告初稿。

**完成内容**

- 新增 `模型报告.md`。
- 报告结构包含摘要、关键词、引言、相关研究、数据集与预处理、方法、实验设置、实验结果、曲线与混淆矩阵分析、讨论、局限性、未来工作、AI 工具使用说明、结论和参考文献。
- 报告明确主结论：Pretrained AST 在 ESC-50 上达到最佳验证 Accuracy 0.9300，明显优于 CNN baseline 和 CNN + SpecAugment。

**遇到的问题**

- 初稿仍为 Markdown 文档，尚未转换为 Word/PDF，也没有完成版式检查。
- 类别级图表仍使用数字类别编号，真实类别名称错误分析需要后续同步 ESC-50 元数据后补充。

**解决方案**

- 先以 Markdown 保持可编辑性，便于继续调整文字、图表和引用。
- 将 Word/PDF 排版、类别名称修复和最终页数控制列入后续整理阶段。

**阶段结论**

- 当前项目已经从代码实验阶段进入报告整合阶段。
- 已有材料足以形成完整模型报告初稿，后续重点是润色、版式和补充真实类别错误分析。

**下一步计划**

- 将 `模型报告.md` 转换为 Word 文档，并检查图表、表格和参考文献格式。
- 如时间允许，补充 ESC-50 真实类别名混淆矩阵和错误类别讨论。

## 2026-06-09 ESC-50 真实类别名图表更新

**今日目标**

- 同步 ESC-50 官方元数据，并重新生成带真实类别名称的混淆矩阵、类别级指标和结果摘要。

**完成内容**

- 新增 `metadata/esc50.csv`，包含 ESC-50 官方 `target` 到 `category` 的映射信息。
- 使用 `metadata/esc50.csv` 重新生成 CNN baseline、CNN + SpecAugment 和 Pretrained AST 三组实验的 `analysis` 输出。
- 更新 `实验结果分析.md` 和 `模型报告.md`，将 `class_00` 等编号说明替换为真实类别错误分析。

**阶段结论**

- 图表和类别级摘要现在可以直接用于报告，AST 相对较弱类别包括 `helicopter`、`pig`、`door_wood_creaks` 和 `airplane`。
- CNN baseline 中多个类别仍为 0 Accuracy，例如 `pig`、`hen`、`sheep`、`chirping_birds` 和 `breathing`，更能说明预训练 AST 的优势。

**下一步计划**

- 将更新后的图表用于 Word/PDF 报告排版。
- 如需进一步增强报告说服力，可围绕 AST 的弱类别补充少量错误样本或类别相似性讨论。

## 2026-06-09 模型报告 Word 初稿生成

**今日目标**

- 将 `模型报告.md` 转换为 Word 文档，形成可继续人工排版和提交检查的报告初稿。

**完成内容**

- 新增 `scripts/build_model_report_docx.py`，用于从 `模型报告.md` 生成 `模型报告.docx`。
- 生成 `模型报告.docx`，包含标题、摘要、表格、训练曲线、AST 混淆矩阵、讨论、局限性、未来工作和参考文献。
- 完成基础结构检查：Word 文档包含 73 个段落、3 个表格和 4 张图片；关键内容如 `摘要`、`实验结果`、`0.9300`、`helicopter` 和 `参考文献` 均已写入。

**遇到的问题**

- 当前本机没有 `soffice` / LibreOffice，无法将 Word 文档渲染为页面 PNG 做视觉级版式检查。

**解决方案**

- 先完成 DOCX 结构生成和内容完整性检查。
- 后续应在安装 Word 或 LibreOffice 的环境中打开 `模型报告.docx`，检查图片尺寸、表格分页、标题层级和参考文献格式。

**阶段结论**

- 当前已具备模型报告 Word 初稿，可以进入人工版式润色和最终提交材料整理阶段。

**下一步计划**

- 打开 `模型报告.docx` 做人工视觉检查。
- 根据课程页数、格式要求调整图表大小、表格宽度、参考文献格式和页面分页。

## 2026-06-09 FSD50K 扩展范围确认与代码准备

**今日目标**

- 根据最新明确要求，将 FSD50K 从“可选未来工作”调整为项目下一阶段扩展实验。

**完成内容**

- 新增 `FSD50KDataset`，支持读取 FSD50K 官方 train / val / eval split，并生成 200 维 multi-hot 标签。
- 新增多标签指标 `multilabel_classification_metrics`，记录 mAP、micro-F1 和 macro-F1。
- 新增 `scripts/prepare_fsd50k.py`，用于检查 FSD50K 目录、样本数和类别数。
- 新增 `configs/fsd50k_ast.yaml`、`scripts/train_fsd50k_ast.py` 和 `slurm/train_fsd50k_ast.sbatch`，为 FSD50K 预训练 AST 多标签微调做准备。
- 更新 `项目计划.md`、`模型报告.md`、`实验结果分析.md` 和 README，将 FSD50K 标记为明确扩展实验。

**阶段结论**

- 项目下一阶段目标已从 ESC-50 单标签分类扩展到 FSD50K 多标签分类。
- FSD50K 的评价指标应从 Accuracy 切换为 mAP、micro-F1 和 macro-F1。

**下一步计划**

- 在服务器下载并解压 FSD50K 到 `data/FSD50K`。
- 运行 `.venv/bin/python scripts/prepare_fsd50k.py --root data/FSD50K` 检查数据目录。
- 提交 `slurm/train_fsd50k_ast.sbatch` 做 FSD50K 多标签 AST sanity run。

## 2026-06-09 FSD50K AST 多标签训练结果

**今日目标**

- 拉取服务器端 FSD50K AST 多标签训练结果，并更新分析文档和报告。

**完成内容**

- 从远端同步 `outputs/fsd50k_ast/history.json` 和 `latest_val_metrics.json`。
- 新增 `scripts/analyze_fsd50k_results.py`，生成 FSD50K 多标签训练曲线和结果摘要。
- 生成 `outputs/fsd50k_ast/analysis/training_loss.png`、`validation_metrics.png` 和 `summary.md`。
- 更新 `模型报告.md`、`实验结果分析.md`、`项目计划.md` 和 README，写入 FSD50K 初步结果。

**实验记录**

| 实验编号 | 数据集 | 模型 | 配置 | 最佳验证 mAP | 最佳轮次 | 输出 |
| --- | --- | --- | --- | --- | --- | --- |
| E006 | FSD50K | Pretrained AST multi-label fine-tuning | `configs/fsd50k_ast.yaml` | 0.6208 | 4 | `outputs/fsd50k_ast` |

**阶段结论**

- FSD50K 多标签 AST 扩展已跑通，最佳验证 mAP 为 0.6208。
- 最佳轮验证 micro-F1 为 0.6955，macro-F1 为 0.4810，说明整体预测能力较好，但长尾类别仍更难。

**下一步计划**

- 将 FSD50K 图表同步到 Word 报告。
- 后续可进一步分析类别级 mAP、阈值敏感性和长尾类别错误。
